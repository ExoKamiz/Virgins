@model IEnumerable<Virgins.Models.Virgin>

<div class="container p-3">
    <div class="five"><h1>List of Real Virgins</h1></div>
    <link rel="stylesheet" href="~/@(nameof(Virgin)).styles.css" />
    <div class="col-6 text-right">
            <a asp-controller="Virgin" asp-action="Create" class="btn btn-primary">
                <i class="fas fa-plus"></i> &nbsp; Registr            <!--добавляем знак плюс с помощью шрифта awesome-->
            </a>
    </div>
    <div class="container">
        <br />
        <div class="row">
            @foreach (var virg in Model)
            {
                <partial name="_VirginsCard" model="virg" />      @*добавили ссылку на частичное представление и модель*@
                <br />
                //display all products
            }
        </div>
    </div>
</div>
    <br /><br />
